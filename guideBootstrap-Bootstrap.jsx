//************************************************//Auteur : Christophe Bernard// script pour photoshop//creation d'une grille Bootstrap 3// possibilité de gere la taille de la goutiere + largeur totale du site//************************************************function createGuidesGrid (gutterWidth, fullWidth){	//creation du document	var docRef = app.documents.add(fullWidth, fullWidth*2, 72,"home");		// variables 	var docActive = app.activeDocument;	var guides = docActive.guides;	var numberOfColumns=12;		//conditions		for(i=0;i<numberOfColumns+1;i++){				var columnWidth = fullWidth/numberOfColumns;		var num = columnWidth* i;		//pour les mobiles		if(fullWidth<480){					numberOfColumns = 1;					guides.add(Direction.VERTICAL,num-gutterWidth);					guides.add(Direction.VERTICAL,num+gutterWidth);					guides.add(Direction.VERTICAL,num);												}			//pour les tablettes en vertical						else if(fullWidth<767){					numberOfColumns = 12;					guides.add(Direction.VERTICAL,num-gutterWidth);					guides.add(Direction.VERTICAL,num+gutterWidth);					guides.add(Direction.VERTICAL,num);												}		else if(fullWidth<980){					numberOfColumns = 12;					guides.add(Direction.VERTICAL,num-gutterWidth);					guides.add(Direction.VERTICAL,num+gutterWidth);					guides.add(Direction.VERTICAL,num);												}		else {				numberOfColumns = 12;				var columnWidth = fullWidth/numberOfColumns;				var num = columnWidth* i;				//				guides.add(Direction.VERTICAL,num-gutterWidth);				guides.add(Direction.VERTICAL,num+gutterWidth);				guides.add(Direction.VERTICAL,num);						}					}}createGuidesGrid(parseInt(prompt("largeur des gouttières", 10)),parseInt(prompt("Largeur totale", 970)) );//createGuidesGrid(10,700);